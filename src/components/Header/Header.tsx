import { IonHeader, IonTitle, IonToolbar } from '@ionic/react';

const Header = () => {
  return (
    <IonHeader>
      <IonToolbar>
        <IonTitle>Users</IonTitle>
      </IonToolbar>
    </IonHeader>
  );
};

export { Header };
